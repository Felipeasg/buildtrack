# Copy to .env and adjust. Used by docker-compose.
# Generate a real secret with: openssl rand -hex 32
SECRET_KEY=replace-with-output-of-openssl-rand-hex-32

# Production database password (used by docker-compose.prod.yml).
# Defaults to "buildtrack" if unset; set a strong value in production.
POSTGRES_PASSWORD=buildtrack
